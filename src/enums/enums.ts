export enum MoveType {
    MOVE, TIMEOUT, COMPLETED, START, INVITE
}
export enum StompType {
    MOVE, MESSAGE, CONNECTION, GAME_CONNECTION
}
export enum EsitoRichiesta {
    INVIATA, ACCETTATA, RIFIUTATA, ANNULLATA
}
export enum NotificationType {
    MESSAGE, EMAIL, REQUEST
}